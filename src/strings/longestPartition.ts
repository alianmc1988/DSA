const longestPartition = (str: string): number => {
	const partitions = []

	for (let i = 0; i < str.length; i++) {
		let startIndex = i
		let endIndex = str.length

		for (let j = startIndex + 1; j <= endIndex - 1; j++) {}
	}
}
